package repository

import (
	// "context"

	// "github.com/george124816/gelection/internal/db"
	"github.com/george124816/gelection/internal/election/model"
)

func Update(election model.Election) error {
	// 	sqlStatement := `
	// UPDATE election SET name = $1 WHERE id = $2
	// `

	// _, err := db.Engine.Exec(context.Background(), sqlStatement, election.name, election.id)
	// if err != nil {
	// 	return err
	// }
	//
	return nil
}
